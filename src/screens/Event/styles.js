import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  firstTitle: { marginBottom: 8 },
  secondTitle: { marginBottom: 24 },
  avatar: { marginRight: 12 },
});

export default styles;
