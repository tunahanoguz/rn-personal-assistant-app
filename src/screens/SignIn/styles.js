import { StyleSheet } from 'react-native';
import { colors } from '../../styles';

const styles = StyleSheet.create({
  appleButton: { marginTop: 40 },
  googleButton: { marginTop: 16, marginBottom: 40 },
  buttonIcon: { marginRight: 20 },
  signUpLink: { marginTop: 16 },
  termsContainer: { borderTopWidth: 1, borderColor: colors.lightGrey },
    termsText: { marginTop: 4 },
});

export default styles;
